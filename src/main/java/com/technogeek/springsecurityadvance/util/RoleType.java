package com.technogeek.springsecurityadvance.util;

public enum RoleType {
    USER, // Default role for regular users
    ADMIN, // Role for administrators
    MANAGER, // Role for managers
    GUEST; // Optional role for guests
}
